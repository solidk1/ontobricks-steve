name: CI

on:
  push:
    branches: [master, main, develop]
  pull_request:
    branches: [master, main, develop]

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  lint:
    name: Lint & Format
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Set up Python
        run: uv python install 3.11

      - name: Install dependencies
        run: uv sync --dev

      - name: Check formatting with black
        run: uv run black --check src/ tests/

      - name: Lint with flake8
        run: uv run flake8 src/ tests/ --max-line-length=100 --extend-ignore=E203,W503

      # Blocking, repo-wide, and deliberately narrow: F821 is an undefined name,
      # which is a NameError waiting for the line to execute — never a style
      # opinion, so it needs no burn-down period. Kept separate from the advisory
      # ruff step below so the whole-repo backlog cannot mask a new one.
      #
      # Ported from upstream 362e40af. It would have caught four in this fork:
      # a comparison against an unassigned `be` in digitaltwin.py and a call to an
      # unimported `resolve_warehouse_id` in DeltaBase.py, both live NameErrors,
      # plus two annotation-only cases.
      - name: Ruff (gate — undefined names, F821)
        run: uv run --frozen ruff check --select F821 --output-format=concise src/ tests/

      # M3.P1 (advisory) — ruff runs in report-only mode against the changed files
      # of the PR. The full repo currently has ~3000 lint findings; we'll burn
      # them down over time. Pre-commit hook gates NEW code (changed lines only).
      - name: Ruff (advisory — changed files only)
        continue-on-error: true
        run: |
          set -e
          if [ -n "${GITHUB_BASE_REF:-}" ]; then
            git fetch origin "$GITHUB_BASE_REF" --depth=1
            files=$(git diff --name-only --diff-filter=ACM "origin/$GITHUB_BASE_REF" \
                    | grep -E '^(src|tests)/.*\.py$' || true)
            if [ -n "$files" ]; then
              echo "Ruff on changed files:"
              echo "$files"
              uv run ruff check $files || true
            else
              echo "No Python files changed in this PR — skipping ruff."
            fi
          else
            echo "No GITHUB_BASE_REF — running ruff on src/ + tests/ in report mode."
            uv run ruff check src/ tests/ || true
          fi

  mypy-diff:
    name: M3.P1 — mypy diff gate (no new errors)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Set up Python
        run: uv python install 3.11

      - name: Install dependencies
        run: uv sync --dev

      - name: Enforce mypy baseline (no NEW errors vs mypy_baseline.txt)
        env:
          DATABRICKS_HOST: https://test.databricks.com
          DATABRICKS_TOKEN: test-token
          DATABRICKS_SQL_WAREHOUSE_ID: test-warehouse
          SECRET_KEY: test-secret-key
        run: uv run python scripts/check-mypy-diff.py

  test:
    name: Tests (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]

    # PostgreSQL 16 for the `db`-marked tests. Without it they skip, so the
    # co-tenancy contract (no extensions, residue-free DROP SCHEMA CASCADE) and
    # the legacy-config migration went unverified in CI.
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: ontobricks_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Set up Python ${{ matrix.python-version }}
        run: uv python install ${{ matrix.python-version }}

      - name: Install dependencies
        run: uv sync --dev

      - name: Run tests with coverage (G1 — unit + integration)
        env:
          DATABRICKS_HOST: https://test.databricks.com
          DATABRICKS_TOKEN: test-token
          DATABRICKS_SQL_WAREHOUSE_ID: test-warehouse
          SECRET_KEY: test-secret-key
          # Runs the `db`-marked tests against the service container above.
          # Docker-in-Docker is unavailable here, so testcontainers cannot be
          # used; the DSN path in the fixture exists for exactly this.
          ONTOBRICKS_TEST_DSN: >-
            host=localhost port=5432 dbname=ontobricks_test
            user=postgres password=postgres sslmode=disable
        run: |
          uv run pytest tests/ \
            --cov=src \
            --cov-branch \
            --cov-report=xml \
            --cov-report=term \
            -v \
            --ignore=tests/e2e \
            -m "not e2e and not property and not eval and not external"

      - name: Upload coverage to Codecov
        if: matrix.python-version == '3.11'
        uses: codecov/codecov-action@v7
        with:
          files: ./coverage.xml
          flags: unit
          name: python-${{ matrix.python-version }}
          fail_ci_if_error: false

      - name: Upload coverage report
        if: matrix.python-version == '3.11' && always()
        uses: actions/upload-artifact@v7
        with:
          name: coverage-report
          path: coverage.xml
          retention-days: 14

  coverage-gate:
    name: Coverage Gate (G1-pkg — per-package thresholds)
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: test
    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Set up Python
        run: uv python install 3.11

      - name: Install dependencies
        run: uv sync --dev

      - name: Download coverage artifact
        uses: actions/download-artifact@v8
        with:
          name: coverage-report

      - name: Enforce per-package thresholds
        run: uv run python scripts/check_coverage.py --xml coverage.xml --config ci/coverage_thresholds.yaml

  mcp-test:
    name: MCP Integration (G1c — when src/mcp-server/ touched)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          fetch-depth: 2

      - name: Detect MCP changes
        id: changes
        run: |
          if git diff --name-only HEAD^ HEAD | grep -q '^src/mcp-server/'; then
            echo "mcp_changed=true" >> "$GITHUB_OUTPUT"
          else
            echo "mcp_changed=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Install uv
        if: steps.changes.outputs.mcp_changed == 'true'
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Set up Python
        if: steps.changes.outputs.mcp_changed == 'true'
        run: uv python install 3.11

      - name: Install dependencies
        if: steps.changes.outputs.mcp_changed == 'true'
        run: uv sync --dev

      - name: Run MCP integration tests
        if: steps.changes.outputs.mcp_changed == 'true'
        env:
          DATABRICKS_HOST: https://test.databricks.com
          DATABRICKS_TOKEN: test-token
          DATABRICKS_SQL_WAREHOUSE_ID: test-warehouse
          SECRET_KEY: test-secret-key
        run: uv run pytest tests/mcp/ -m mcp -v --tb=short

  build:
    name: Build Check
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Set up Python
        run: uv python install 3.11

      - name: Install dependencies
        run: uv sync --dev

      - name: Build package
        run: uv build

      - name: Verify package
        run: |
          ls -la dist/
          echo "Build artifacts created successfully"

  docs:
    name: Documentation Build
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Set up Python
        run: uv python install 3.11

      - name: Install dependencies
        run: uv sync --dev

      - name: Build Sphinx docs
        run: |
          cd documentation/sphinx
          uv run make html || echo "Sphinx build completed with warnings"

  security:
    name: Security Scan
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Set up Python
        run: uv python install 3.11

      - name: Install dependencies
        run: uv sync --dev

      - name: Audit Python dependencies (pip-audit)
        run: |
          uv run pip-audit --requirement requirements.txt --strict \
            --output pip-audit-results.json --format json || true
          uv run pip-audit --requirement requirements.txt --strict
